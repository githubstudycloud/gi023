# Augment Code Configuration

本项目使用多 AI Agent 协作开发框架。
阅读 `.agents/INSTRUCTIONS.md` 获取完整协作规则。按 `.agents/ONBOARDING.md` 完成入驻。

核心约束：只在自己分支开发，只修改自己的 registry/signals 文件，
提交加 Co-Authored-By，UTF-8 编码，不直接提交 main。
