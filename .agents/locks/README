# Locks 目录
# 当多个 Agent 操作同一目录时使用文件锁
# 锁文件格式: {resource-name}.lock
# 锁内容为 JSON: {"agent": "...", "acquired_at": "...", "purpose": "..."}
#
# 锁规则:
#   1. 创建锁前先检查是否已存在
#   2. 锁文件存在 = 资源被占用
#   3. 操作完成后必须删除锁文件
#   4. 锁超过 10 分钟视为过期，可强制释放
#   5. 锁文件不应被 git 追踪（已加入 .gitignore）
